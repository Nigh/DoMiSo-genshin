{
  "name": "domiso-universal-frontend",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "app",
    "packages/core",
    "packages/player",
    "packages/dialog-hooks"
  ],
  "scripts": {
    "dev": "npm run dev -w app --",
    "build": "npm run build -w packages/player && npm run build -w packages/dialog-hooks && npm run build -w app",
    "build:dev": "npm run build -w packages/player && npm run build -w packages/dialog-hooks && npm run build:dev -w app",
    "test": "npm run test -w app"
  },
  "dependencies": {
    "jotai": "2.18.0"
  },
  "overrides": {
    "jotai": "2.18.0",
    "jotai-effect": "2.2.3"
  }
}
